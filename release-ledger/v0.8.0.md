# Codex Token Bar v0.8.0 Unified Release Ledger

Status: **published and publicly verified for macOS arm64 + Windows x64/ARM64.**

- Prepared: 2026-07-23 Asia/Shanghai.
- Binary source commit: `eda0e7b6ff30e3839d61e682f43439181147ce24`.
- Binary source archive SHA256: `e3962ee0f8538d613e4ea358a21a200a3105ef7941f87d2f0a74ff01d0e7bfb4`.
- Release branch: `release/v0.8.0`.
- Annotated tag: `v0.8.0`, fixed at `db489b55c9e102c7a519c99d1046cd8363ef4905`.
- Remote baseline: `origin/main` at `535bbcd5f65a90202bcd78424fb436cf6b24137a`.
- GitHub Release: `https://github.com/hututuo/codex-token-bar/releases/tag/v0.8.0`.
- Published: `2026-07-23T15:59:35Z`.
- GitHub latest release: `v0.8.0`.

## Source Baseline

This candidate includes both bounded-history-memory repairs:

- Swift native scanner: `14ccc58`.
- Tauri/Rust scanner and cache: `1903256`.
- Candidate metadata/appcast ledger: `eda0e7b`; no binary-source changes after `1903256`.

The Tauri repair places hard limits on session bytes, JSONL line size, event counts, cache shard bytes/counts, per-session fingerprints, and directory file counts. It rejects changed files and malformed or over-limit scans rather than presenting a partial precise total. Oversized input falls back to safe metadata instead of retaining unbounded history in memory.

Validation on this source baseline:

- Rust library tests: **571 passed, 4 ignored, 0 failed**.
- Tauri frontend tests: **469 passed, 0 failed**.
- Tauri production frontend build: passed (159 modules).
- Targeted token-history tests on macOS: **63 passed, 0 failed**.
- Targeted token-history tests on Windows x64: **63 passed, 0 failed**.
- Release-script suite: **25 passed, 1 skipped, 0 failed**; the skip is the PowerShell-only fixture on macOS.
- `npm audit --omit=dev`: 0 vulnerabilities.
- `git diff --check`: passed.
- `cargo fmt --check` was not run because the local stable toolchain lacks `rustfmt`; no global component was installed.

## macOS Candidate

- Version `0.8.0`, build `800`, bundle ID `local.codex.token-bar`, `arm64`.
- App and embedded Sparkle framework passed deep strict `codesign` verification.
- The exact DMG passed `hdiutil verify`.
- The generated appcast enclosure signature validates against the exact versioned ZIP.
- The exact final DMG was mounted and opened in Finder; the user subsequently authorized proceeding with publication preparation, which is recorded as the visual-approval gate.
- The candidate is ad-hoc signed and is not Apple-notarized. Gatekeeper may show an unidentified-developer prompt; this is the existing release policy.

## Windows Candidate

The LAN build host is `HTTWIN` at the currently discovered address `192.168.124.44`. The stale address `192.168.123.44` was routed through `utun5`/`198.18.0.1`; proxy/VPN synthetic port responses made an offline PC appear reachable before SSH closed without a banner. After the PC was powered on, SMB/mDNS resolved the real LAN address and the existing SSH key worked.

- Clean build root: `C:\Users\HTT\CodexTokenBarV080MemoryGuardEda0e7bB`.
- Toolchain: Node `24.17.0`, npm `11.13.0`, rustc/cargo `1.96.0`.
- Source archive transferred and independently hash-matched.
- The first OpenSSH-child `Start-Process` approach was terminated when the SSH job ended; no output from it was used.
- The corrected one-time scheduled task completed both builds and was then deleted and verified absent.
- npm clean install audited 36 packages with 0 vulnerabilities.
- x64 application: PE `0x8664`, ProductVersion `0.8.0`, SHA256 `bbca8bcda73232caeabee7759d6aa29c5fd3053d9a9f6b8a9f3ea157fb6d6889`.
- ARM64 application: PE `0xAA64`, ProductVersion `0.8.0`, SHA256 `a0934d4a4fa561083c438ead927ea8c9d0e7a261b8cc02dfc13bcbf2604be68d`.
- Both installers were updater-signed on macOS with the private key remaining Mac-only.
- Production release validation passed.
- Tauri wraps each detached signature in Base64. Direct verification of the wrapper correctly failed with `InvalidEncoding`; after decoding to minisign text, independent `minisign-verify 0.2.5` verification passed for both installers.
- `latest-windows.json` contains exactly `windows-x86_64` and `windows-aarch64`, both pointing to the intended `v0.8.0` GitHub asset URLs.

## Final Upload Set

Directory: `dist/release/v0.8.0/upload/`

| Asset | Bytes | SHA256 |
|---|---:|---|
| `CodexTokenBar-v0.8.0-macos-arm64.dmg` | 8,436,088 | `42bd2eb6d4b98bbb1794af26b46580dd94857a448fe7b9127c54ffb358cd709d` |
| `CodexTokenBar-v0.8.0-macos-arm64.app.zip` | 7,604,402 | `8ee15897125f6bb06983fc2d155b4530a07b05732f0eda1311bf650abc471917` |
| `CodexTokenBar.app.zip` | 7,604,402 | `8ee15897125f6bb06983fc2d155b4530a07b05732f0eda1311bf650abc471917` |
| `CodexTokenBar-v0.8.0-windows-x64-setup.exe` | 5,431,155 | `a343637fdb5e27955695eed8be25c240709bb2e4925661ec18e3c8982b56888e` |
| `CodexTokenBar-v0.8.0-windows-x64-setup.exe.sig` | 436 | `95e6117969ee0e34a070d679fb03e6afce73ec796f265cb4e1284b861c130f94` |
| `CodexTokenBar-v0.8.0-windows-arm64-setup.exe` | 4,745,010 | `d00b9bd036a773a6febc7dbc23972d4b708bcb382fb3ee16ec3897d20bf0246f` |
| `CodexTokenBar-v0.8.0-windows-arm64-setup.exe.sig` | 440 | `2246b59c31efc9f50a3ccfa4c1588fb6c03ebafad0ddec29bf5289a331ca6ed7` |
| `latest-windows.json` | 1,368 | `8a6860a7f075303314fcb9a4a3e17833028d08f4ddf966adf2445066a1b67165` |
| `SHA256SUMS-v0.8.0.txt` | 832 | `bf0152da300da771daa0b35073402211a1ece1894033dc46c5edb3b12d266361` |

The unified checksum file covers the eight payload/update files preceding itself and verifies 8/8. `appcast.xml` is source-controlled and is not a GitHub Release asset.

## Publication Result

- The user explicitly authorized publication after the full upload review packet.
- The release branch and annotated tag were pushed and both resolved to `db489b55c9e102c7a519c99d1046cd8363ef4905`.
- A draft Release was created with exactly the nine reviewed assets.
- All nine draft assets were downloaded again; every file was byte-identical to the reviewed local upload set and the unified checksum passed 8/8.
- The Release was published as latest, then `main` was fast-forwarded to the reviewed release commit.
- All nine public assets were downloaded again and were byte-identical to the reviewed local upload set.
- The public raw `main/appcast.xml` was byte-identical to the reviewed appcast.
- The public `releases/latest/download/latest-windows.json` was byte-identical to the reviewed Windows metadata.
- The next changelog boundary is the immutable `v0.8.0` tag, not any later audit-only commit.

## Evidence

- Swift memory guard: `_keep/projects/codex-token-dashboard/runs/20260722-233058_swift-usage-scan-memory-guard/`.
- Tauri memory guard: `_keep/projects/codex-token-dashboard/runs/20260723-223849_tauri-usage-scan-memory-guard/`.
- Fresh Windows rebuild/signing: `_keep/projects/codex-token-dashboard/runs/20260723-232811_v0.8.0-windows-rebuild/`.
- Draft and public GitHub downloads, public feed copies, and byte-verification evidence are retained under that same Windows rebuild run.
- Earlier `20260721-090656_v0.8.0-unified-release` assets are superseded and must not be published.
