# Codex Token Bar v0.8.0 Unified Release Ledger

Status: **prepared locally; awaiting final DMG visual approval**.

- Prepared: 2026-07-21 Asia/Shanghai.
- Branch: `release/v0.8.0`.
- Asset source commit: `cbbc23de287b421be935e3cfc7e8e240d55b6dd4`.
- Tag: not created.
- GitHub Release: not created.
- Release notes: `release-notes/v0.8.0.md`.
- Upload gate: no branch, tag, Release, or asset has been pushed. Publication requires final DMG visual approval and a separate upload-review authorization.

## Version And Source

- macOS Swift: version `0.8.0`, build `800`, bundle ID `local.codex.token-bar`, architecture `arm64`.
- Windows Tauri: version `0.8.0`, x64 and ARM64.
- Exact Windows source archive: `codex-token-bar-cbbc23d.tar.gz`.
- Source archive bytes: `9,120,657`.
- Source archive SHA256: `df8bac4b7ad1895ea24cc3d10c036e163fb104092a303f95f7903ebb142a746a`.
- The archive was produced by `git archive` from the exact asset source commit and contained no `.git`, `node_modules`, or Rust `target` directory.
- Windows PowerShell parsed the two repository Windows release scripts with zero errors before the build.

The first archive, from `ee675bc`, exposed a Windows-only Win32 type mismatch in `process_is_alive`. Commit `cbbc23d` explicitly compares the `u32` exit code with `STILL_ACTIVE as u32`. The final archive, all Windows binaries, and the final macOS package use the corrected source baseline.

## Integrated Source Gates

Before final packaging on the release branch:

- Node and release tooling: **499 passed, 1 skipped, 0 failed** out of 500.
- Rust: **563 passed, 4 ignored, 0 failed**.
- Swift: **714 passed, 3 skipped, 0 failed**.
- Tauri production frontend build: passed, 159 modules.
- `npm audit --omit=dev --registry=https://registry.npmjs.org`: 0 vulnerabilities.
- Git LFS integrity check: passed.
- Release security preflight: passed.
- `git diff --check`: passed.

The local stable Rust toolchain does not have `rustfmt`, so `cargo fmt --check` could not run. No macOS-global Rust component was installed. The final source change is a one-token Windows-only cast; the corrected Windows source compiled the complete test binary, ran the release-focused Windows suites, and produced both target installers.

## macOS Package

- `scripts/build_release.sh v0.8.0` was rerun after the final Windows compatibility commit.
- The App reports version `0.8.0`, build `800`.
- Sparkle framework and the App passed deep strict `codesign` verification.
- The final DMG passed `hdiutil verify`, writable/remounted/compressed content gates, App and Applications-link checks, and background-file checks.
- Current Finder stores the picture using `pBB0`/`pBBk` bookmark records instead of the legacy `backgroundImageAlias`; the final `.DS_Store` contains both bookmark records and the `dmg-background.png` reference. Final human visual approval remains pending.
- Sparkle generated a new EdDSA enclosure signature and `sign_update --verify` passed for the exact versioned zip.
- macOS remains ad-hoc signed and is not Apple notarized, matching the README and release notes.

## Windows Build And Runtime

The corrected exact source archive was built in the Parallels Windows 11 ARM64 VM with Node `24.14.0`, npm `11.9.0`, rustc/cargo `1.96.0`, Visual Studio Build Tools `17.14.36`, MSVC x64/ARM64, Windows SDK, and LLVM.

- Provider session-copy Windows tests: **3 passed**.
- Rollout file-identity fail-closed test: passed.
- Updater registry focused tests: passed.
- x64 release App: PE `0x8664`, ProductVersion `0.8.0`, 21,101,056 bytes, SHA256 `c478c6501815b8a067ec513e6df5a4c0202499c1153becd166893072bc939e5d`.
- ARM64 release App: PE `0xAA64`, ProductVersion `0.8.0`, 17,698,816 bytes, SHA256 `610e227e3ecb50348f3f1f567ead40a23d5ef477a30365225d4fd78bcff853d3`.
- The unsigned installer manifest passed strict version/platform/arch/name/bytes/SHA and exact-directory validation before leaving Windows.

The exact x64 release executable also passed an interactive runtime smoke:

- primary main window appeared;
- a secondary instance exited with code 0 and activated the retained primary;
- exactly one candidate process remained after secondary activation;
- candidate process count returned to 0 after the test;
- settings, update-monitor state, and the user Run key were restored exactly.

## Windows Build-Host Toolchain Change

The VM no longer contained the toolchain used for v0.7.3, so this release restored official build-only dependencies inside the VM:

- Visual Studio Build Tools 2022 at `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`, including VCTools, x64, ARM64, Windows SDK, and LLVM.
- Rustup under `C:\Users\huyiyang\.cargo` / `.rustup`, with release default `1.96.0-aarch64-pc-windows-msvc` and x64 plus ARM64 targets.
- Node was not installed globally; the build used the existing Codex-bundled runtime.

Rollback commands are retained in `vs-build-tools-install-result.json` and `rust-toolchain-pin-1.96-result.json` in the release evidence run. The VM did not require a reboot.

## Windows Updater Signing

- The updater private key remained on the Mac and was never copied to Windows, Git, or the release run.
- Both detached signatures passed the production envelope/metadata validators and independent streaming verification with `minisign-verify 0.2.5` against the decoded current public key.
- `latest-windows.json` contains exactly `windows-x86_64` and `windows-aarch64`; each stable `v0.8.0` URL is correct and each embedded signature exactly matches its detached `.sig` file.
- Installer bytes remain bound to the validated manifest; the generic NSIS outer stub is not used to infer the application architecture.

## Local Upload Assets

The exact nine review assets are under `dist/release/v0.8.0/upload/`. This directory is ignored by Git.

| Asset | Bytes | SHA256 |
|---|---:|---|
| `CodexTokenBar-v0.8.0-macos-arm64.dmg` | 8,880,564 | `9d286e266a4ce8d19d198b0a8c218b865b08e3e250b3b2fb246c1c08ce64f9d8` |
| `CodexTokenBar-v0.8.0-macos-arm64.app.zip` | 7,583,462 | `79428c0ebf0f3aefa3676e0cf56d4c71c34286b5ad8ca351e94b3241a2ddd888` |
| `CodexTokenBar.app.zip` | 7,583,462 | `79428c0ebf0f3aefa3676e0cf56d4c71c34286b5ad8ca351e94b3241a2ddd888` |
| `CodexTokenBar-v0.8.0-windows-x64-setup.exe` | 5,451,936 | `b354f34724d53e22730b46835baac0c07d9d9208f7cd3ab205de5cf2ee90f5bf` |
| `CodexTokenBar-v0.8.0-windows-x64-setup.exe.sig` | 436 | `d19e02128ee583229e143df660912e08b99ec199d66a0f9c3a0ca680b5873a9c` |
| `CodexTokenBar-v0.8.0-windows-arm64-setup.exe` | 4,749,594 | `8cec89eaeac07def8e2ca6db9519fb7f2de1e6766ac5408bd604e6e1f421f54b` |
| `CodexTokenBar-v0.8.0-windows-arm64-setup.exe.sig` | 440 | `2bd71abf73b52bb0ac541b074d2c12f32d4f39d2fb9034e3106354f47b0590d1` |
| `latest-windows.json` | 1,368 | `979773e7854ee2487fcc8e5fb3944aecfc85f8fadfd31d4f76a6f7185d3b5533` |
| `SHA256SUMS-v0.8.0.txt` | 832 | `d1d1c05e2f9037023cc26da177ae5b7a1fca58448dce77bee30931d9ed14844c` |

`SHA256SUMS-v0.8.0.txt` covers the first eight assets and passed all eight checks. It does not include itself.

## Update Metadata

- Source `appcast.xml`: 35,842 bytes, SHA256 `8aa03ee27b67b13b34837b64cde0f92dba7a5353eb763663393fa30e5a3627e0`.
- Newest Sparkle item: version `0.8.0`, build `800`, enclosure length `7,583,462`, URL `https://github.com/hututuo/codex-token-bar/releases/download/v0.8.0/CodexTokenBar-v0.8.0-macos-arm64.app.zip`.
- The appcast keeps five versions: `0.8.0`, `0.7.3`, `0.7.2`, `0.7.0`, and `0.6.0`.
- Windows continues to read the separate `latest-windows.json` Release asset and does not consume the Sparkle appcast.

## Evidence

- Release run: `_keep/projects/codex-token-dashboard/runs/20260721-090656_v0.8.0-unified-release/`.
- The run retains both exact source archives, preflight evidence, the Win32 type-mismatch discovery, toolchain install/pin evidence, final Windows build result/transcript, runtime smoke, task XML, unsigned build manifest, updater public key, decoded signatures, and the independent signature verifier.

## Planned Publication

1. Open the exact final DMG and wait for explicit visual approval.
2. Present the complete version, branch/tag, release-notes, nine-asset, checksum, signing, update-feed, and command review packet; wait for explicit upload authorization.
3. Push the reviewed release commit, create annotated tag `v0.8.0`, create a draft GitHub Release, and upload exactly these nine assets.
4. Re-download the draft assets and compare them byte-for-byte before publishing as latest.
5. Fast-forward public `main` only after the Release assets exist, then verify public appcast, Windows metadata, all public assets, and the stable Release state.
