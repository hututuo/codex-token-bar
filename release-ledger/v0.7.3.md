# Codex Token Bar v0.7.3 Release Ledger

Status: **source candidate / not published**.
Prepared: 2026-07-11 Asia/Shanghai.
Branch: `release/v0.7.3`.
Integrated source baseline: `2339662dd702ada93fa1424150ebcd356d1d7428`.
Release notes: `release-notes/v0.7.3.md`.

## Candidate Scope

- Tauri product metadata and the Windows release-build default are aligned to `0.7.3`.
- The candidate includes the reviewed cross-platform source/lifecycle/cache work, Swift background CPU and unread-scan reductions, chart navigation/accessibility fixes, Tauri tray status panel and native live rate, Windows hidden autostart and single-instance restoration, Provider Repair safety, app-level update monitoring, and Windows file-identity compatibility described in the release notes.
- This ledger records source readiness only. No v0.7.3 installer, signature, checksum, updater metadata, tag, or remote Release is asserted to exist.

## Existing Integration Evidence

Commander completed the integrated source suites at baseline `2339662` before this release-material change:

- Node: **388 passed, 1 skipped**.
- Rust: **498 passed**.
- Swift: **545 passed**.

This version-and-documentation pass intentionally runs only release-focused consistency and script gates, plus `git diff --check`; it does not repeat the full suites or build/open either app.

## Remaining Gates

1. Run both macOS candidates and verify their visible runtime behavior, source switching, lifecycle recovery, charts, tray/status surfaces, and update presentation.
2. Compile the latest Windows source for x64 and ARM64 on the target Windows toolchain; verify binary version/machine identity and the two-stage release output.
3. Install the Windows candidate and validate real system notification behavior, persistent tray badge/menu fallback, hidden autostart, and single-instance restoration. Notification support remains a post-build installed-runtime gate.
4. Generate the real macOS and Windows assets, then verify hashes, architecture, package identity, updater signatures, and absence of private keys.
5. Only after assets pass review, update `appcast.xml`, `latest-windows.json`, stable download references, checksums, and final asset accounting.
6. Obtain explicit publication authorization before creating/pushing a tag, uploading assets, publishing a GitHub Release, or advancing remote stable metadata.

## Deliberately Unchanged

- `appcast.xml` still describes the published v0.7.2 Sparkle release.
- `latest-windows.json`, README stable download names, and existing `dist/` assets remain untouched.
- No private updater key or real release asset is added to Git.
