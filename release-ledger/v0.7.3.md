# Codex Token Bar v0.7.3 Unified Release Ledger

Status: **published and remotely verified**.

- Prepared: 2026-07-13 Asia/Shanghai.
- Published: 2026-07-13 05:29:04 UTC.
- Branch: `release/v0.7.3`.
- Asset source commit: `0c4977461db54d99e0a94ee6203d5053dd4c40c3`.
- Annotated tag `v0.7.3`: `820a7e72cf4a5801331d07dcab43a7ab0cff917a` after dereference.
- Release: `https://github.com/hututuo/codex-token-bar/releases/tag/v0.7.3`.
- Release notes: `release-notes/v0.7.3.md`.
- Full report: `audits/v0.7.3/reports/optimization-report.md`.

The release branch, annotated tag, GitHub Release, nine assets, Sparkle appcast,
and Windows metadata are public. The Release is neither draft nor prerelease
and is GitHub's current latest release.

## Version And Source

- macOS Swift: version `0.7.3`, build `703`, bundle ID
  `local.codex.token-bar`, architecture `arm64`.
- Windows Tauri: version `0.7.3`, x64 and ARM64.
- Windows source archive: `codex-token-bar-0c49774.tar.gz`.
- Source archive bytes: `8,862,890`.
- Source archive SHA256:
  `a3791041cd755973609a66f13d3159ab6288c7d2cf8b2ab1d193490660d6bb82`.
- The archive was created with `git archive` from the exact source commit and
  contained no `.git`, `node_modules`, or Rust `target` directory.
- Windows PowerShell 5 parsed both release scripts with zero errors before the
  build.

## Integrated Source Gates

Fresh suites on the integrated release branch:

- Swift: **579 passed, 0 failed**.
- Rust: **501 passed, 0 failed**.
- Node: **387 passed, 0 failed**.
- Swift `swift build --disable-sandbox`: passed.
- Rust `cargo check --locked --lib`: passed with existing dead-code warnings.
- Tauri `npm run build`: passed, 152 modules.
- Release tooling Node tests: **25 passed, 1 skipped**; the skip is the tracked
  PowerShell fixture because this Mac has no `pwsh`/`powershell`.
- `git diff --check`: passed.

The current stable Rust toolchain does not have the `rustfmt` component, so
`cargo fmt -- --check` could not run. No global toolchain component was
installed for this release. The Rust changes compiled and passed the complete
library suite.

## macOS Package

- `scripts/build_release.sh v0.7.3` produced the current Swift App, DMG,
  versioned Sparkle zip, compatibility zip, appcast, and checksum from the
  asset source commit.
- The App reports version `0.7.3`, build `703`.
- Sparkle framework and App passed deep strict `codesign` verification.
- The final DMG passed the Finder background persistence gate, RW remount,
  compressed read-only remount, and `hdiutil verify`.
- Sparkle generated a new EdDSA enclosure signature and `sign_update --verify`
  passed for the exact versioned zip.
- macOS remains ad-hoc signed and is not Apple notarized, matching the README
  and release notes.

## Windows Build And Runtime

The exact source archive was built on `HTTWIN` with Node `24.17.0`, npm
`11.13.0`, rustc/cargo `1.96.0`, Visual Studio Build Tools, and the target-aware
LLVM/MSVC wrapper.

- Provider session-copy Windows tests: **3 passed**.
- Rollout file-identity fail-closed test: passed.
- Updater registry focused tests: passed.
- x64 release App: PE `0x8664`, ProductVersion `0.7.3`, 19,953,664 bytes,
  SHA256 `19c8269ace39b2603325e9ead92e57ae2694493fb74a10b49d4a1608617da6de`.
- ARM64 release App: PE `0xAA64`, ProductVersion `0.7.3`, 16,753,664 bytes,
  SHA256 `222e99422d83bf478072cbe7288550b6f149d078b40514a84ef0846917e9e95a`.
- The unsigned installer manifest passed strict version/platform/arch/name/
  bytes/SHA and exact-directory validation before leaving Windows.

An exact x64 release-exe smoke ran in the real interactive user session without
replacing the user's existing v0.7.1 installation:

- primary main window appeared;
- manual secondary exited with code 0;
- one primary process remained after activation;
- candidate process count returned to 0;
- settings, update-monitor state, and the Run key were restored exactly;
- both one-time scheduled tasks were unregistered.

The deeper autostart/destroyed-main/repeated-activation runtime scenarios were
already passed by the unchanged startup implementation during the earlier
v0.7.3 Windows gate. This final pass revalidated the exact current executable's
main-window and single-instance path.

## Windows Updater Signing

- The updater private key remained on the Mac and is not present in Git,
  Windows input, or the release directory.
- The local key uses an explicitly empty password; the production signer passed
  the required explicit empty argument without putting a nonempty secret in
  argv or logs.
- Installer bytes were treated as opaque; architecture stayed bound to the
  validated manifest rather than the generic NSIS outer PE stub.
- Both detached signatures passed envelope validation and real streaming
  verification with `minisign-verify 0.2.5` against the public key embedded in
  `tauri.conf.json`.
- `latest-windows.json` contains exactly `windows-x86_64` and
  `windows-aarch64`; each metadata signature exactly matches its detached
  `.sig` file.
- Windows metadata publication timestamp: `2026-07-13T04:15:52Z`.

## Local Upload Assets

All upload paths are under `dist/release/v0.7.3/`. The directory is ignored by
Git and is not part of the release commit.

| Asset | Bytes | SHA256 |
|---|---:|---|
| `CodexTokenBar-v0.7.3-macos-arm64.dmg` | 7,936,032 | `ef678e144f63820908d699e38a5b61fd3736a63946f49cecb49cf96b9c72602f` |
| `CodexTokenBar-v0.7.3-macos-arm64.app.zip` | 6,618,673 | `aac4ff605a43f06c2f4e7c883a02130116ccbf47b4ec990c0b7db1a56751026a` |
| `CodexTokenBar.app.zip` | 6,618,673 | `aac4ff605a43f06c2f4e7c883a02130116ccbf47b4ec990c0b7db1a56751026a` |
| `CodexTokenBar-v0.7.3-windows-x64-setup.exe` | 5,142,653 | `700dd51396d8daa933904fe2badd68c7ac2aad0edf36773cf443d3572b4c958a` |
| `CodexTokenBar-v0.7.3-windows-x64-setup.exe.sig` | 436 | `3f82459ee1a54e8881b2e9a275c4b06212b65d504293a9bace4ea53846333ea5` |
| `CodexTokenBar-v0.7.3-windows-arm64-setup.exe` | 4,464,591 | `7d8449ed938a5b299e7e77a4146dda5c3d2d6b8b681cd9aeb425ff122b88a02a` |
| `CodexTokenBar-v0.7.3-windows-arm64-setup.exe.sig` | 440 | `d80a8dfa714a864d79d0579d6224980ad614ebea7f77725c8e2dd2c1e2619ff3` |
| `latest-windows.json` | 1,368 | `7c03b9e7f75f48f3cc8675b76b5146632494bcb0fc71f0647418af7a4cf3b86a` |
| `SHA256SUMS-v0.7.3.txt` | 832 | `fbb0ca1c95890726ab37b6c791c8cbf27ca1771b9338a2273df59346f948b07f` |

`SHA256SUMS-v0.7.3.txt` covers the first eight assets and passed all eight
checks. It does not include itself.

## Update Metadata

- Source `appcast.xml`: 31,178 bytes, SHA256
  `d90d54f23964d3d6368b552496df6a1bd56ba90a27be9695a9fe0c6c3dba3ba9`.
- Newest Sparkle item: version `0.7.3`, build `703`, enclosure length
  `6,618,673`, URL
  `https://github.com/hututuo/codex-token-bar/releases/download/v0.7.3/CodexTokenBar-v0.7.3-macos-arm64.app.zip`.
- The appcast keeps the newest five total versions: `0.7.3`, `0.7.2`, `0.7.0`,
  `0.6.0`, and `0.5.2`.
- Windows continues to read the separate `latest-windows.json` Release asset;
  it does not consume the Sparkle appcast.

## Evidence

- Full engineering report:
  `audits/v0.7.3/reports/optimization-report.md`.
- Current release run:
  `_keep/projects/codex-token-dashboard/runs/20260713-115450_v0.7.3-optimization-release/`.
- The run retains the source archive/hash, macOS build log, Windows preflight,
  build result/transcript, runtime smoke result, task XML, signing log, real
  signature-verification log, and unsigned Windows build inputs.

## Publication And Remote Verification

1. The user explicitly authorized publication after the final DMG was opened
   and the adaptive 5h/7d behavior was reverified in Swift and Tauri.
2. `release/v0.7.3` and annotated tag `v0.7.3` were pushed. The tag dereferences
   to the exact reviewed commit above.
3. A draft Release received exactly the nine assets in this ledger. All nine
   were downloaded from the draft and matched the local candidate byte-for-byte
   before publication.
4. The Release was published as latest. All nine assets were downloaded again
   from the public stable `v0.7.3` URLs and matched byte-for-byte; the public
   checksum passed 8/8.
5. Remote `main` was fast-forwarded only after the Release assets existed. The
   public `main/appcast.xml` is byte-identical to the reviewed appcast with
   SHA256 `d90d54f23964d3d6368b552496df6a1bd56ba90a27be9695a9fe0c6c3dba3ba9`.
6. Public `latest-windows.json` reports `0.7.3`, contains only
   `windows-x86_64` and `windows-aarch64`, uses stable `v0.7.3` URLs, and its
   signatures exactly match the public detached signature files.

Public re-download evidence is retained under the current release run in
`remote-verification-820a7e7/` and `public-verification-v0.7.3/`.
