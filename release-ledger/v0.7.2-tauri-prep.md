# Codex Token Bar v0.7.2 Tauri Release Prep

Status: Windows x64/ARM64 assets built, returned, locally updater-signed, and assembled into the unified local candidate; not published.

## Scope

- Lane: Windows / Tauri cross-platform update.
- Tauri source branch: `fix/tauri-floating-tray-actions`.
- Tauri candidate head: `7ef1b96`.
- Unified branch: `release/v0.7.2`.
- Integrated source commit used for Windows build: `3b7a5c48f9463369453eaca41c8b29212a31ba77`.
- Source archive transport SHA256: `9139a83bfca6866a29fc8781542f2a8483e1f96b1c4ce9ff8f4e5deaedcb8063` before and after transfer.
- Product version: `0.7.2`.
- Release notes: `release-notes/v0.7.2.md`.

## Included Tauri Follow-ups

- Codex CLI discovery supports explicit `CODEX_CLI_PATH`, LaunchServices, bounded system/user Applications scans, known compatibility candidates, Homebrew, and PATH without relying on one App name.
- App-bundle candidates must carry `CFBundleIdentifier = com.openai.codex`; wrong, missing, or invalid bundle metadata is rejected.
- The maintained Codex Desktop sidebar patch helper uses explicit-path and bundle-identity-based discovery.
- Same-source trusted usage summaries remain visible during ordinary session appends while one background aggregate refresh runs; live-rate reads no longer scan the full session tree each tick.
- The candidate also includes the accepted fork/cache, quota, Radar, Provider Repair, refresh cadence, compact surface, chart, source-propagation, and capability-boundary fixes documented in `release-notes/v0.7.2.md`.

## Windows Build Provenance

- Host: `HTTWIN` / `HTTWIN\\HTT` over the established private network path.
- Toolchain: Node `24.17.0`, npm `11.13.0`, Rust `1.96.0`; x64 and ARM64 Rust targets present.
- Visual Studio x64 tools and Windows 11 SDK were already installed.
- `Microsoft.VisualStudio.Component.VC.Tools.ARM64` was added and is intentionally retained for future Windows ARM64 builds. It can be removed later through Visual Studio Installer if rollback is required.
- Temporary scheduled task `CodexTokenBar-v0.7.2-Build` was deleted after the build.
- Updater private key material never left the Mac. `createUpdaterArtifacts` was temporarily disabled on Windows, installers were returned to the Mac, and the existing local Tauri updater key produced the final `.sig` files.
- Raw App verification: x64 machine `0x8664`, ARM64 machine `0xAA64`; ProductVersion/FileVersion both `0.7.2`.
- Build evidence: `/Users/huyiyang/AI agent/Codex/_keep/projects/codex-token-dashboard/runs/20260710-110529_windows-v0.7.2-build/`.

## Final Windows Assets

Canonical upload copies are in `dist/release/v0.7.2/`:

| Asset | Bytes | SHA256 |
|---|---:|---|
| `CodexTokenBar-v0.7.2-windows-x64-setup.exe` | 4,584,723 | `7d61b63fed28f004da45d710d3524f648a29295115396046c3f3aba21e02d7ea` |
| `CodexTokenBar-v0.7.2-windows-x64-setup.exe.sig` | 436 | `91a8f582ec2580966e2c57d29a4ef70b6cae873a5c659505a7d033a150c40f0b` |
| `CodexTokenBar-v0.7.2-windows-arm64-setup.exe` | 3,996,431 | `767dba06629b9e48ba62f39721bb995ced40c6f4f5ce5d260ad9e60f82fcd823` |
| `CodexTokenBar-v0.7.2-windows-arm64-setup.exe.sig` | 440 | `6de6ae5aba5681ce4c5ccbfb4604d2e1f5c09676d2fffba6b9ebf6b446134dd5` |
| `latest-windows.json` | 1,368 | `86bbdf7d36bfe2079cf18cd14b34a6ed659a3dce664e0b67ac21d019444847dd` |

The unified `SHA256SUMS-v0.7.2.txt` is 832 bytes with SHA256 `a4eb397f5f226787d3c93ec7ecf68155a250af845c531187789f37563c67da17` and covers all Mac and Windows release assets plus `latest-windows.json`.

## Candidate Verification

- The source candidate previously passed the full frontend/script suite (270 tests), full Rust suite (194 tests plus doc tests), production frontend build, release preflight, capability/CSP/updater checks, and artifact/private-key guards.
- This asset-accounting pass did not repeat full suites because it changes release documentation only.
- `shasum -a 256 -c SHA256SUMS-v0.7.2.txt` passed for all eight listed release targets.
- `latest-windows.json` reports version `0.7.2`, contains `windows-x86_64` and `windows-aarch64`, and points both entries to the same `v0.7.2` GitHub Release.
- Embedded signature lengths match the sidecar files: 436 for x64 and 440 for ARM64.
- The release `dist/` directory remains ignored and no private key is tracked.

## Publication Boundary And Review Packet

- Target repository: `hututuo/codex-token-bar`.
- Single planned tag: `v0.7.2`, pointing to the final reviewed `release/v0.7.2` head.
- Upload the two Windows installers, two `.sig` files, `latest-windows.json`, the three Mac assets, and the unified checksum file to one draft GitHub Release.
- Use `release-notes/v0.7.2.md` as the Chinese-first / English-second body.
- Publish the GitHub Release only after draft asset/hash review. Publish `appcast.xml` through a reviewed fast-forward of `main` afterward, then verify both update channels remotely.
- Exact non-executed commands are recorded in `release-ledger/v0.7.2.md`.

## Remaining Gates

1. Commander reviews the final release-material commit and complete local assets.
2. User/commander explicitly authorizes push, tag, GitHub Release upload/publication, and update metadata publication.
3. Remote verification confirms every asset hash, the Sparkle enclosure, and both Windows updater platform entries after publication.

No push, tag, GitHub Release, upload, `appcast.xml` publication, or `latest-windows.json` publication is authorized by this record.
