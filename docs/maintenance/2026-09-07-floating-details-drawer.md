# 悬浮窗纵向运行模型详情（2026-09-07）

点击主／子任务数字后，详情与主卡片保持同宽，优先向下展开；底部空间不足时向上展开。根据当前显示器可用区域限高，长列表在详情内滚动。关闭后恢复打开前的主卡片位置。

详情标题下显示“Esc 或 × 关闭 · 再次点击主／子数字也可收起”。Swift 仅在详情打开期间允许悬浮面板接收键盘；Tauri 同时处理卡片内及窗口级 Escape。保留既有失焦和空白点击关闭行为。

边缘吸附与保持展开独立：详情只暂停自动收起，不解除吸附。原生窗口调整尺寸前保留黑框，调整后更新吸附区域；临时详情尺寸和位置不进入持久化位置。未吸附的悬浮窗打开详情时，也不会因变高而自动吸附到另一个边缘。Tauri 的尺寸和位置调整复用原生原子帧更新命令。

Swift 在 `FloatingPanelMetrics.swift` 中计算纵向尺寸和方向，`FloatingDetailsDrawerTests.swift` 覆盖短屏限高、命中区域、Esc 和吸附生命周期。Tauri 在 `floatingWindowPlacement.ts` 中计算位置，详情组件测量自然内容高度并限制滚动区域。黑框包住两部分；打开时黑框扩展，详情内容随后显现。

已通过本次相关 Swift 测试 117 项、前端测试 91 项。最终独立副本构建、签名、替换与源文件哈希以 `runs/20260907-floating-edge-dock/drawer-activation.json` 为准。交互验收由用户接手；代理未操作实际界面，Windows 编译及设备验收未运行。

此次只处理详情抽屉及其边缘吸附兼容，不包含工作目录中其他并行任务的价格和数字格式修改；未合并 main、推送或发布。
