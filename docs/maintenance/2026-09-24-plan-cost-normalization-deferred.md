# 计划费用均一化：延期记录

状态：`DEFERRED_NOT_IN_RELEASE`

本次版本只展示原始 API 价格估算，不展示或使用“均一化”金额。GPT-6 与 GPT-5.6 的官方价格卡仍保留，均一化系数和说明不进入本次发布路径。

保留的实现已通过分支 `codex/plan-cost-normalization-future` 保存，后续版本再单独评审和接回。

重新启用前的约束：

1. 明确均一化金额的业务定义、适用模型和价格版本，不把它当成官方账单。
2. 保持原始 API 价格、未知价格和独立额度三者的展示边界。
3. 新增 UI、序列化和回归测试后，再重新打开均一化展示。
