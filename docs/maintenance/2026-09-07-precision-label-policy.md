# 精确统计完成态与既定排除策略

用户选择沿用现有分项计数和上下文估值排除策略，不增加历史核对面板、不全量重建、不反复扣减已排除数字。

- Swift `.precise` 统一显示“精确统计”，不再因旧版 `accountingCoverage` 标记长期显示橙色“已确认小计”；元数据状态及真实同步/失败进度保持原流程。
- Tauri 正常完成态同样显示“精确统计”。后台停止为按既定策略排除的记录追加永久小计警告；真实扫描、迁移失败及其他警告保留。
- 原始日志、统计库内容、解析规则、指纹、增量检查点不修改。已有历史迁移已将不计数记录排除；本次不会再扣一次，也不会把内部历史标记篡改为 complete。
- 用户认可此日志统计口径；“精确统计”指当前解析模式完成，不构成对上游日志未提供的压缩用量或实际订阅账单的保证。

验证及本地构建/运行记录见 `runs/20260907-precision-label/`。界面验收继续由用户完成。
