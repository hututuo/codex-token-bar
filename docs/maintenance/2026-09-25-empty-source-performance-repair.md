# 两万文件压力回归：历史、修复与交接

工作分支：`fix/v092-audit-sidebar`。应用版本：`0.9.2`。本记录不代表正式安装器或自动更新发行已验收。

## 历史原因

`d4a6fc59a59c1449ce310363fc7420e27d87847b`（2026-07-24）引入原规模压力回归：20,000 个空 JSONL 加一个含 120 Token 的 JSONL。它验证文件枚举不会因文件数量上限截断，不能通过降低文件数量解决。

`5ce004190e5fbc20195d017da873646135817142`（2026-09-03，schema 11 迁移）删除小文件直接处理分支，改为所有完整重建都进入私有持久暂存池。原意是统一暂存恢复机制，让小文件也可使用并行解析工作线程；主库仍按确定顺序、单写入者导入。这些隔离和恢复目标应保留。

副作用是首次发现的零字节文件也需要各自创建 SQLite 暂存库、准备结构、持久同步、写清单、校验、导入和清理；没有 Token 内容，仍支付逐文件持久化成本。Windows 文件系统上的大量独立文件操作放大了成本。此前 HTTWIN 测试超过约 27 分钟后被停止，不能记为通过，也不能据此断言永久死锁。

这不是 F02 的文件物理身份检查导致的主要退化，不应撤销物理标识/ctime 检查，也不应关闭 fsync、降低 SQLite 持久性或回退 schema。

## 已提交的修复

`c0bd89934b85b21883a8dcb3ea9fdcb3cf7b15ec` 为首次发现、没有同路径或同会话历史来源的零字节文件增加独立路径：

- 每批最多 128 个文件，检查来源身份后在主库的待发布代际事务中登记元数据、空前缀和 offset=0 检查点。
- 导入前重新核对打开句柄和当前路径。已增长、被替换或绑定历史身份的文件交回普通解析与账本协调。
- 不逐空文件建立私有暂存库；非空及既有来源继续走原暂存/恢复流程。
- 批次失败整体回滚；批次提交仍不等于代际发布。中断后可以复用已提交的待发布检查点。
- 不跳过空文件。以后追加继续从检查点解析；删除或截断有历史记录的文件不清空永久账本。

原规模回归保留全部 20,001 个文件，并增加工作量断言：仅一个非空文件建立暂存库，20,000 个空文件经过 157 次批次提交；热刷新没有暂存库创建、正文扫描或内容校验字节。

核心位置：

```text
tauri-app/src-tauri/src/core/usage/token_count_jsonl/exact_usage_index/empty_sources.rs
tauri-app/src-tauri/src/core/usage/token_count_jsonl/exact_usage_index/empty_sources/tests.rs
tauri-app/src-tauri/src/core/usage/token_count_jsonl/tests.rs
```

## 本次继续工作完成的补充

`47b3b7e943e75c984bd94e6656c40e0a1bdc529a` 修正 Windows 全量回归中两项既有源码契约测试：Git 检出为 CRLF 时，固定 `\n` 的字符串断言误报。先在本机用 LF/CRLF 两种实际源码表示复现相同两项失败，再仅归一化测试读取的换行；未更改生产锁权限、共享方式、相对路径或重解析点保护。检查只取生产源码段，避免断言中的同名字符串自证通过。

该提交另补两项生产路径回归：空检查点提交后、代际发布前出现追加；排队后的新空文件在导入前消失。前者在下一次扫描准确增量计入，后者不留下虚假来源。删除后已发布 Token 仍保留。

之前未提交的引导组件集成测试、跨平台版本检查及校验和范围说明已在 `c0bd8993` / `07984b25` 提交。保留的本地 stash 是与远端合并前的替代实现备份，不是需要重复应用的补丁。用户既有 `HANDOFF.md` 未修改。

## 验收边界与接手清单

自动回归和实际交互分开记录，不用跳过用例或缩小夹具冒充修复。最终运行记录追加在本文末尾。

人工验收使用包含上述提交的同一候选，重点如下：

1. Windows 与 Swift 的额度侧栏固定、刷新、主页按钮；贴边展开/收起、拖动、详情关闭后的真实命中区域；Windows 混合 DPI、多屏及引导全部图文和退出后的尺寸恢复。
2. 旧版升级后的旧显示偏好、默认新侧栏和现有历史；用隔离旧库验证真实用户数据，尤其原始 JSONL 已删除而账本仍存在的记录。不删除用户索引来“解决”迁移问题。
3. 正式安装包的内部版本、选定架构、签名、更新清单、覆盖安装、自启、退出/重启；明确 ARM64 的实际发行/更新范围。本次只提交代码，不更新线上 appcast、不发布 tag/Release、不替换用户当前运行包。

HTTWIN 上的机器特定性能与真实交互仍由验收确认。不同主机的耗时不直接计算加速倍数。

此前 `~/tmp/ctb-finish-fixes-20260925/` 中的 Windows 0.9.2 验证二进制对应旧的 `0d60821d`，不包含后续空文件批处理。接手验收时必须从本文记录的最终源代码重新构建，不能把旧验证包当作当前候选。Swift 本轮没有进一步生产代码变更。

## 本机自动回归（代码候选 47b3b7e9）

| 项目 | 本次结果 |
| --- | --- |
| 两项 Windows 源码契约 | 修正前用 CRLF 夹具复现两项失败；修正后 LF/CRLF 均通过，原安全断言不变 |
| 协调文件系统专项 | 5 通过、0 失败 |
| 空来源专项 | 7 通过、0 失败，包含本次新增的两项竞态 |
| macOS 主机 Rust 原生库全量 | 1,142 通过、0 失败、10 ignored，无额外过滤；96.17 秒 |
| 前端 Node 全量 | 1,138 通过、0 失败 |
| 发布辅助 Node 全量 | 91 通过、0 失败、1 skipped（本机无 PowerShell） |
| 前端生产构建 | 通过 |
| 原规模压力用例单独重跑 | 20,001 来源全部保留；冷扫描 15.650 秒、热刷新 1.363 秒；用例总计 17.95 秒 |

专项与全量包含重叠用例，不把它们相加宣称独立覆盖数。10 个 ignored 用例仍应按各自实际外部夹具要求另行验收，不冒充运行通过。日志保存在本机 `~/tmp/ctb-pressure-finish-20260925/`；其中 `crlf-before-fix.log` 是故意保留的修正前失败证据，不是最终失败结果。

## Windows 自动回归最终结果

GitHub Actions 运行 `36121228118`，代码 SHA `47b3b7e943e75c984bd94e6656c40e0a1bdc529a`，Windows job `108026933519` 已完成，结论为 **success**。本轮使用托管 `windows-latest`，不冒充 HTTWIN 的机器特定实测。

```text
https://github.com/hututuo/codex-token-bar/actions/runs/36121228118
```

已确认成功的步骤：前端构建、七项空来源安全回归、原规模 20,001 文件压力回归、完整原生库回归、引导与版本回归。完整原生库命令没有额外过滤或跳过压力用例；未将常规 ignored 测试标记为执行通过。最终 Windows 结果以该运行的 job/step 回执及附属日志为准，不从前次失败运行外推通过数量或耗时。

对照的旧运行 `36113505034` 在 `07984b25` 上已经通过压力测试，但全量库因两项 CRLF 源码断言失败；本次修正后完整流程成功。GitHub Actions 的缓存保存步骤因命中缓存而跳过，不属于测试跳过。

本记录及 `2026-09-25-repair-completion.md` 的后续提交仅更新文档；生产代码与测试以已通过的 `47b3b7e9` 为准。本轮不再遗留需要提交的代码或测试。
