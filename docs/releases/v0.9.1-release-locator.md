# Codex Token Bar v0.9.1 发布定位与审计记录

状态：双平台本地发布包已准备，DMG Finder 外观已由用户确认；尚未推送、打 tag、创建 GitHub Release 或上传资产。

## 源码与版本

- 仓库：`https://github.com/hututuo/codex-token-bar`
- 当前分支：`codex/radar-countdown-consistency-20260823`
- 发布准备提交：`7fe7c4e471188997a142b14f141c2d1c2abbe53a`
- 审计记录提交：`b9376d13f37958fc335fd8883c7ca7d4437d1343`
- 浮动窗 IQ 分割线修复提交：`baa2171067a5afacbdaaebe33ba20a88cc61709a`
- 发布版本：`0.9.1`
- macOS build：`901`
- 计划 tag：`v0.9.1`
- 上一公开版本：`v0.9.0`
- Sparkle feed（源码配置）：`https://raw.githubusercontent.com/hututuo/codex-token-bar/main/appcast.xml`

## 本地发布资产

目录：`dist/release/v0.9.1/`

| 文件 | SHA256 |
| --- | --- |
| `CodexTokenBar-v0.9.1-macos-arm64.dmg` | `6d4e2861a20499b602a5307746a0939a538ec9ad7031bca9a7b4901dd08376b4` |
| `CodexTokenBar-v0.9.1-macos-arm64.app.zip` | `b41970bef3333018c0a47c371c3ab35787a63d824fe796ae46b7a5a297a367e9` |
| `CodexTokenBar.app.zip` | `b41970bef3333018c0a47c371c3ab35787a63d824fe796ae46b7a5a297a367e9` |

中间清单：`SHA256SUMS-v0.9.1-macos.txt`。

Windows 资产（签名后复制到发布目录根部）如下：

| 文件 | SHA256 |
| --- | --- |
| `CodexTokenBar-v0.9.1-windows-x64-setup.exe` | `72cd1db9b2347cba2d4291eddbd8028f15c22e32bf9fad7785f38b19f20c787d` |
| `CodexTokenBar-v0.9.1-windows-x64-setup.exe.sig` | `d06910ff9425c47ae538777f5261efb384d19a15304aec6fc680b9da6723710a` |
| `CodexTokenBar-v0.9.1-windows-arm64-setup.exe` | `ea01b207a76f16a1570e81e3667d398515b69bda1525bc9143183a42655fc165` |
| `CodexTokenBar-v0.9.1-windows-arm64-setup.exe.sig` | `6a3d9a4df84b509af2fa291dc8d48ac8c34ebe94de47fdb1dde69ef698a49c38` |
| `latest-windows.json` | `3f0099ce86850d245ff783221909121e72337604e311b00c07317d4d6db00d74` |

统一清单：`SHA256SUMS-v0.9.1.txt`，自身 SHA256 为 `85a02f5398dfe9eee9c9d38cd5686020eb3b58aa553de6ab06eae0f54cfb7b3e`，覆盖前述 8 项资产。

Windows 构建来源：

- 主机：`HTTWIN` / `192.168.124.44`，用户 `htt`。
- 源码归档：提交 `7fe7c4e471188997a142b14f141c2d1c2abbe53a`，归档 SHA256 `8734831add6082b8cd30380843cd05bad134ccb5fbaa341da31e0f099f9c0f0d`。
- 未签名构建目录：`C:\Users\HTT\CodexTokenBarV091Head7fe7c4e4\dist\release\v0.9.1\windows-build`；签名只在 Mac 本地完成，updater 私钥未离开 Mac。
- 构建完成后只清理了本次生成的旧/失败 Rust `target`、临时归档和旧失败源码副本；Node/Cargo 依赖缓存保留。

## 门禁结果

- Swift 全量测试：`1361` 执行，`0` 失败，`4` 跳过；选区摘要布局测试已调整为有足够高度的宿主窗口，避免宿主不足时 AppKit 的几何居中干扰交互层断言。
- Node 发布测试：`33` 通过，`0` 失败，`1` 跳过（本机没有 `pwsh`）。
- 注入脚本语法检查：通过。
- `npm audit --omit=dev --audit-level=high`（官方 npm registry）：`0 vulnerabilities`。
- Windows 构建主机：`HTTWIN` / `192.168.124.44`；Node `24.17.0`、npm `11.13.0`、Rust `1.96.0`；x64/ARM64 target、LLVM/NSIS、WebView2 预检通过。
- Windows `cargo test --locked`：`945` 通过、`5` 忽略、`0` 失败；x64/ARM64 NSIS 构建均通过。
- Windows 生产依赖审计：`0 vulnerabilities`。完整开发依赖审计仍报告 `nanoid` 与 `postcss` 共 `2` 个 high，均来自开发依赖；未把 `npm audit fix` 的未经验证升级带入发布。
- Windows updater：x64/ARM64 `.sig` 已由本地 updater key 生成，并由签名器执行 key-id、Blake2b-512/Ed25519 和 trusted-comment 验证。
- macOS App：ad-hoc 签名；`codesign --verify --deep --strict` 通过；版本 `0.9.1`、build `901`；`spctl` 拒绝属于当前未 notarize 策略的预期警告。
- Sparkle：appcast 版本/build 元数据通过；本地 macOS zip 的 EdDSA 签名验证通过。
- DMG：`hdiutil verify` 通过；应用、`Applications` 链接和背景图存在；当前 macOS 将 Finder 背景写入 `.DS_Store` 的新版 `pBB0blob/pBBkblob` 记录，并引用 `dmg-background.png`，已通过等价检查。旧校验脚本只识别历史 `backgroundImageAlias`，其单项报错不代表 DMG 结构失败。

## 双平台发布边界

README 承诺同时提供 macOS、Windows x64 和 Windows ARM64。三条资产线现在都已在本地就位；统一 checksum 已对账通过。Windows 未使用商业 Authenticode 证书，首次安装仍可能出现 SmartScreen 提示。

## 待用户确认与后续动作

1. 用户已在 Finder 中检查并确认 DMG 安装窗口外观。
2. 双平台本地资产与版本号已核对，review packet 已更新。
3. 执行 tag、GitHub Release、Release 资产上传，并把已确认的 `appcast.xml` 与统一 checksum 提交到目标分支。

尚未执行的外部动作：`git push`、创建 tag、创建 GitHub Release、上传 DMG/zip 或更新远端 Sparkle feed。
