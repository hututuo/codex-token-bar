# Codex Token Bar v0.9.1 发布定位与审计记录

状态：双平台本地发布包已准备，等待用户检查 DMG Finder 外观；尚未推送、打 tag、创建 GitHub Release 或上传资产。

## 源码与版本

- 仓库：`https://github.com/hututuo/codex-token-bar`
- 当前分支：`codex/radar-countdown-consistency-20260823`
- 发布准备提交：`ca6031c967d0928673c030794a767243ac939ace`
- 浮动窗 IQ 分割线修复提交：`baa2171067a5afacbdaaebe33ba20a88cc61709a`
- 发布版本：`0.9.1`
- macOS build：`901`
- 计划 tag：`v0.9.1`（待用户确认 DMG 后再执行）
- 上一公开版本：`v0.9.0`
- Sparkle feed（源码配置）：`https://raw.githubusercontent.com/hututuo/codex-token-bar/main/appcast.xml`

## 本地发布资产

目录：`dist/release/v0.9.1/`

| 文件 | SHA256 |
| --- | --- |
| `CodexTokenBar-v0.9.1-macos-arm64.dmg` | `3b661e2f505de68a941b2e853807c1451525bd3a951513c167a74d751f013cd2` |
| `CodexTokenBar-v0.9.1-macos-arm64.app.zip` | `fa6cb76e9464ff28d5a143f644e9ea286923a8902c0b6cf8c8d992e127c89fb9` |
| `CodexTokenBar.app.zip` | `fa6cb76e9464ff28d5a143f644e9ea286923a8902c0b6cf8c8d992e127c89fb9` |

中间清单：`SHA256SUMS-v0.9.1-macos.txt`。

Windows 资产（签名后复制到发布目录根部）如下：

| 文件 | SHA256 |
| --- | --- |
| `CodexTokenBar-v0.9.1-windows-x64-setup.exe` | `7a836cf90e4cd97352c06f0202aa88b9638834ff9cb3ac4c4eecea5ead5cac63` |
| `CodexTokenBar-v0.9.1-windows-x64-setup.exe.sig` | `1d4b2071251a8b0fdf41d06659c32ef4b157f624832745f56f52dc7740e8f403` |
| `CodexTokenBar-v0.9.1-windows-arm64-setup.exe` | `b067993b5465681cc93f2ac502fb9778d0aceff70267154eb2a16771e49084b8` |
| `CodexTokenBar-v0.9.1-windows-arm64-setup.exe.sig` | `9b4b4217f8001a8117dbce4360b3b9912c9ed90273c16632809fe0ae3cd56248` |
| `latest-windows.json` | `cd0eb292108e2ada1fbb2d7a08b30fe6844784c4700e60b73bfcd0cbda3d289e` |

统一清单：`SHA256SUMS-v0.9.1.txt`，自身 SHA256 为 `aa0014dff76022e07669012a5fe5c802e6e157c8e99cbbee122cc414a2b5c665`，覆盖前述 8 项资产。

## 门禁结果

- Swift 全量测试：`1339` 执行，`0` 失败，`4` 跳过；选区摘要布局测试已调整为有足够高度的宿主窗口，避免宿主不足时 AppKit 的几何居中干扰交互层断言。
- Node 发布测试：`33` 通过，`0` 失败，`1` 跳过（本机没有 `pwsh`）。
- 注入脚本语法检查：通过。
- `npm audit --omit=dev --audit-level=high`（官方 npm registry）：`0 vulnerabilities`。
- Windows 构建主机：`HTTWIN` / `192.168.124.44`；Node `24.17.0`、npm `11.13.0`、Rust `1.96.0`；x64/ARM64 target、LLVM/NSIS、WebView2 预检通过。
- Windows `cargo test --locked`：`925` 通过、`5` 忽略、`0` 失败；x64/ARM64 NSIS 构建均通过。
- Windows 生产依赖审计：`0 vulnerabilities`。完整开发依赖审计仍报告 `nanoid` 与 `postcss` 共 `2` 个 high，均来自开发依赖；未把 `npm audit fix` 的未经验证升级带入发布。
- Windows updater：x64/ARM64 `.sig` 已由本地 updater key 生成，并由签名器执行 key-id、Blake2b-512/Ed25519 和 trusted-comment 验证。
- macOS App：ad-hoc 签名；`codesign --verify --deep --strict` 通过；版本 `0.9.1`、build `901`；`spctl` 拒绝属于当前未 notarize 策略的预期警告。
- Sparkle：appcast 版本/build 元数据通过；本地 macOS zip 的 EdDSA 签名验证通过。
- DMG：`hdiutil verify` 通过；应用、`Applications` 链接和背景图存在；当前 macOS 将 Finder 背景写入 `.DS_Store` 的新版 `pBB0blob/pBBkblob` 记录，并引用 `dmg-background.png`，已通过等价检查。旧校验脚本只识别历史 `backgroundImageAlias`，其单项报错不代表 DMG 结构失败。

## 双平台发布边界

README 承诺同时提供 macOS、Windows x64 和 Windows ARM64。三条资产线现在都已在本地就位；统一 checksum 已对账通过。Windows 未使用商业 Authenticode 证书，首次安装仍可能出现 SmartScreen 提示。

## 待用户确认与后续动作

1. 用户在 Finder 中检查并确认 DMG 安装窗口外观。
2. 用户确认双平台本地资产与版本号后，生成最终上传 review packet；再等待单独的上传/发布指令。
3. 最终上传阶段才执行 tag、GitHub Release、Release 资产上传，并把已确认的 `appcast.xml` 与统一 checksum 提交到目标分支。

尚未执行的外部动作：`git push`、创建 tag、创建 GitHub Release、上传 DMG/zip 或更新远端 Sparkle feed。
