# v0.9.2 双端统一 Schema 11 与无损性能升级记录

状态：Swift 与 Tauri 的本地实现和 macOS 门禁已完成；尚未修改产品版本号，尚未推送、合并、打 tag、上传或发布。Windows x64/ARM64 构建机门禁仍待执行，因此本文不是发布完成声明。

## 兼容边界

前向升级同时覆盖 GitHub 正式版 v0.9.1（tag `6e2889f8ecef771719eb58cab0366f505500c2c5`）和本分支上一统计提交 `fba33820`：

| 端 | 可读取输入 | v0.9.2 写入 | 兼容行为 |
| --- | --- | --- | --- |
| Swift exact index | schema 6 / 7 | schema 11 | 在同一缓存 namespace 构建候选库；复制并转换 SQLite 数据，迁移期 JSONL parser 为 0 |
| Swift fast snapshot | payload v1 / v2 / v3 | payload v4 | root-only lastGood 可在不枚举 JSONL、不打开 exact DB 时读取；成功刷新后只写 v4 |
| Tauri exact index | schema 9 / 10 | schema 11 | 候选库迁移到稳定整数 `source_id`；保留 published、building、checkpoint、chunk、aggregate 和 staging 连续性 |
| Tauri session catalog | schema 1 / 2 | schema 2 | 旧目录可升级；按 thread ID / `updated_at` 增量 upsert，不再绑定文件系统身份 |
| Tauri staging manifest | schema 1 / 2 | schema 3 | 旧 manifest 可读取；忽略物理身份字段，校验 checkpoint 后转换或继续 |
| Tauri dashboard cache | v18 / v20 / v21 | v22 | 同路径 lastGood 可直接首屏显示；成功刷新后只写 v22 |
| Tauri integrity receipt | v1 / v2 | v2 | 绑定规范化路径、DB/WAL 大小与 mtime、schema、revision、published generation；失配只触发校验，不删除数据库 |

v0.9.2 不双写 v0.9.1 的 device/inode/volume 字段。v0.9.2 索引交给 v0.9.1 时，只保证旧版安全拒绝且不改写、不删除；不承诺旧版继续增量。更早或未来未知 schema 同样保留原库并停止精确刷新。

## 缓存优先与统计连续性

- Swift 先走 root-only lastGood，Tauri 先调用 cache-only 启动读取；首屏可显示“上次统计 · 正在更新”，随后后台 single-flight 迁移、校验和增量刷新。
- 未完成当前验证的 lastGood 只用于展示数字和图表，不提升为 attribution 或用户文件写操作的安全凭据。后台失败时继续保留 lastGood，并返回结构化 `repairRequired`。
- 两端 exact fingerprint 统一为 `版本字节 + 11 个 unsigned LEB128` 的可逆 BLOB。它是精确编码，不是概率哈希；模型、reasoning 等字段差异仍能精确区分。
- 统计链路只依据规范化路径、session ID、大小、mtime、首尾 probe、4 MiB chunk hash、parser checkpoint、resume offset 和已提交前缀。device ID、inode/File ID、卷序列号、Codex Home physical identity 和 ctime 不参与统计命中、增量或缓存隔离。

文件分类保持数据事实优先：

- 路径、大小、mtime 相同：`unchanged`，不读 JSONL 正文。
- 文件变大：验证 checkpoint、旧尾块和轮换审计块后，只解析新增后缀。
- 大小相同但 mtime 改变：先比较轻量 probe，再以固定 64 KiB 缓冲流式复核该文件已有 4 MiB chunks；内容一致时不跑 JSONL parser。
- 前缀或 chunk 不一致、文件截断：只重建该文件。
- 新文件：正常首次解析。
- 缺失文件：只有完整枚举成功后，才随新 generation 原子发布单文件墓碑。
- 路径下文件对象被替换但内容完全一致：视为同一份认可数据。

扫描期间继续从已打开句柄读取。若文件继续追加，只提交扫描起点前已验证的前缀，新后缀留到下一轮；发布前复核失败只废弃本轮未发布结果。

## Schema 11 存储与性能

Swift 保留原有稳定 `source_id`、events、chunks、checkpoint、aggregate 和 attribution 体系。schema 6/7 候选迁移转换 fingerprint 和相关绑定，不遍历 JSONL。

Tauri schema 11 将路径和 session ID 集中到 `sources`；events、fingerprints、chunks、enrichment、file aggregates 和 turn candidates 只引用稳定整数 `source_id`。当前发布数据与 building 变化通过 current/pending overlay 表达，验证完成后由单写者事务合并并更新 `published_generation`，append 不再复制该文件全部旧 events/fingerprints/chunks/aggregates。旧 schema 9/10 中同一路径残留的历史 generation 物理副本会压缩为最新 published 逻辑态；`published_files`、`published_events` 与统计总量必须保持一致。

Tauri 所有轻文件重建都进入最多 4 worker 的解析池，不再以 4 MiB 为串行分界；主库仍只有一个 writer。ready artifact 同时受数量和 512 MiB 总字节门禁限制，单个大于等于 512 MiB 的文件继续走 heavy lane。Swift 保留原有 2/3/4 worker 自适应和 heavy lane。完整目录枚举仍负责 tombstone，只是移出首屏关键路径。

`session_metadata` 按 thread ID / `updated_at` 增量 upsert，只有完整读取成功后才移除明确消失项。正式 title 原样保留；只有 first-message/preview 回退文本执行有界展示裁剪。

## 候选迁移与非破坏恢复

双端都在相同缓存目录使用受管候选库、迁移 manifest 和 rollback artifact：

1. 旧活动库只读；迁移前检查活动 DB、WAL、候选预估和额外 512 MiB 安全余量。
2. 候选直接复制 published、building、checkpoint、probe、4 MiB chunks、aggregates、attribution、catalog 和可证明的 staging 状态。
3. 候选通过逻辑行数、event/fingerprint digest、aggregate 总量、checkpoint、generation lineage、`foreign_key_check`、`quick_check` 和 parser-zero 对账。
4. 关闭连接并 fsync 候选和目录后，原子切换；schema 11 重新打开并再次校验。
5. 旧库只在 schema 11 成功打开并完成一次 no-op/append refresh 后退役。失败或崩溃会保留活动库、候选、manifest、rollback 和 staging，供下次继续或报告人工修复。

fingerprint 对账按 `(generation, path)`/`source_id` 分组，把旧 88-byte 编码先转换为 canonical LEB128，并在每个 source 内按 canonical bytes 排序后计算 digest。这样验证的是完全相同的 fingerprint 多重集合，不会因旧 little-endian BLOB 与新 LEB128 的 SQL 排序不同而误报；内存只随单个 source 的 fingerprint 数量增长。

identity 漂移、未知 schema、迁移失败、`quick_check` 失败、DB/WAL 收据失配、权限/磁盘/锁/I/O 失败、枚举不完整、staging 恢复失败以及内容 probe/checkpoint 不一致，都只能返回 lastGood 或 `repairRequired`，不能删除活动库、整表清空或清理缓存 namespace。会话删除、Provider Sync、项目移动等用户文件写操作继续使用原有安全保护，不属于本次解耦范围。

## 验证结果

### 自动化与构建

| 门禁 | 结果 |
| --- | --- |
| Swift 全量测试 | PASS：1388 executed，4 skipped，0 failed |
| Swift release build | PASS |
| Rust 聚焦迁移测试 | PASS：schema 9、schema 10、building 恢复、lagging turn、同计数候选损坏拒绝 |
| Rust `cargo test --locked` | PASS：1021 passed，5 ignored，0 failed |
| Tauri Node 全量测试 | PASS：942 passed，0 failed |
| Tauri TypeScript/Vite production build | PASS |
| Rust release build | PASS |
| diff whitespace 检查 | PASS |

全量套件覆盖 cache-first 首屏、共享 fingerprint golden vectors、小文件并行上限、单 writer、ready 内存门禁、append 仅后缀写入、未知 schema、分阶段迁移/切换失败、空间不足、损坏/失配收据、不可读文件/目录、staging 中断、单文件墓碑，以及会话删除、Provider Sync 和项目移动回归。

### Swift 真实 schema 7 索引副本

- 活动索引只读基线：400,781,312 bytes，schema 7，2778 sources。
- 隔离副本迁移到 schema 11 后为 233,426,944 bytes（58.24%，满足不超过旧库 75% 的门禁）。迁移后有 445062 events、3,731,055 fingerprints、14,166 chunks、17,027 turns，`quick_check=ok`。
- cold 60.369 秒：full parse 0；现场一个增长文件执行 1 次 incremental parse。统计为 73,814,871,658 tokens、445062 calls、2630 threads。
- 紧接的 warm 1.371 秒：新增 full/incremental parse 均为 0，token/call/thread delta 均为 0。
- 活动索引在验收后仍为 schema 7；测试仅操作隔离副本。

### Tauri 真实 schema 10 索引副本

- 活动索引只读克隆基线：1,339,469,824 bytes，schema 10，2778 published files、445103 published events、无 building generation。
- schema 11 cold 208.257 秒：full-body bytes 0，只对实时增长后缀读取 1,886,573 bytes；published generation 122→123，文件 2778→2778，事件 445103→445114。
- 紧接的 warm 2.106 秒：additional full-body、append 和 metadata-validation bytes 全为 0，revision 不变。
- 最终副本 257,642,496 bytes（19.23%，满足不超过旧库 60% 的门禁）；2778 sources、445114 published events、3,731,107 fingerprints、14,166 chunks、17,584 turns，`quick_check=ok`，所有 pending 表为 0，candidate/manifest/rollback 均已按成功路径退役。
- 第一次候选验证曾准确拦截 raw-BLOB 排序导致的 fingerprint digest 误差：旧 schema 10 活动副本保持原样，失败候选与 manifest 被保留。修正为 canonical 分组排序并加入 `1`/`256` 逆序向量后，聚焦测试和真实副本重跑通过。
- 真实活动库最终再次只读确认仍为 schema 10、`quick_check=ok`，且不存在 schema 11 candidate/manifest/rollback；测试只操作 `/tmp` 克隆，验收副本随后已移除。

### v0.9.1 回退安全

在 v0.9.1 tag 的 detached 临时 worktree 中，仅把既有 future-schema 测试 fixture 的值从 `999` 改成精确的 `11`：

- Swift：PASS。旧版拒绝打开，events、sources、attribution ledger、schema 值与 published generation 均不变。
- Tauri：PASS。旧版拒绝打开，events、files、fingerprints、chunks、schema 值与 published generation 均不变。

临时测试改动已恢复，detached worktree 已移除；tag 和当前分支没有被该回退测试改写。

### 尚未完成的平台门禁

- BLOCKED：本机仅安装 `aarch64-pc-windows-msvc` Rust target；此前检查在依赖 `ring` 编译阶段因缺少 Windows SDK C 头文件 `assert.h` 停止。这是 macOS 交叉编译环境缺失，不是当前 Rust 源码测试失败。
- NOT_RUN：Windows x64/ARM64 构建机上的候选切换、文件锁、崩溃恢复、卷序列号漂移注入、schema 9/10→11、增量和完整安装包运行验收。

Windows 两条构建机门禁在发布前仍必须补齐。复杂视觉验收继续由用户负责；本补丁没有新增视觉方案。

## 发布边界

本文记录的是 v0.9.2 本地实现候选。未授权且未执行：修改产品版本号、推送、合并、改 tag、生成或上传正式资产、更新 feed、创建 GitHub Release 或公开发布。
