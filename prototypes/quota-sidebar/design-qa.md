# 额度侧栏原型 QA

final result: passed

范围：本地网页交互原型。参考用户提供的 2000×2000 截图，按最新用户要求改为三层交互；并非像素级复刻，桌面背景、内容和尺寸按本项目重新组织。实际在 Codex 内置浏览器约 678×810 的可见区域检查。

视觉比较：参考图与最终第三层截图已在同一工具输出中并列提供并检查。沿用深黑贴边主体、彩色额度环、深色详情面板和细额度条；用户要求的第一层改为竖向色条。未复刻参考的壁纸、其他产品标志和尖角卡片尾部。

发现并修复：初版详情卡在 570px 桌面预览中底部裁切；改为以可用桌面中央定位，最后截图确认卡片位于菜单栏以下，底部完整可见。

交互实测：
- 重载默认显示第一层竖条。
- 实际 mouseMoved 移入边缘，仅出现第二层额度环，详情仍关闭。
- 点击 5h 环，第三层额度详情出现。
- 前版已验证详情到外部移出收起、独立开关；三层改版保留其关闭路径。
- 控制台错误检查无错误。

限制：这里只验证浏览器原型；原生透明窗口、真实屏幕吸附、多显示器不属于本版。自动演示是使用相同界面状态的模拟指针，不是系统鼠标。
